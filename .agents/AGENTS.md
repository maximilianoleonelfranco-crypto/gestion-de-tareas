# Reglas del Proyecto: Gestor de Tareas

- **Sincronización Android:** Cada vez que se haga un cambio en el código fuente de la aplicación (React/CSS/HTML), se debe ejecutar el siguiente comando para compilar y sincronizar con Capacitor para que Android Studio reciba los cambios:
  `npm run build ; npx cap sync`
